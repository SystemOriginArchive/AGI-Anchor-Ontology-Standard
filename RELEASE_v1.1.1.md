# LockLayer Overlay v1.1.1 — Release Notes

## 핵심 정의 (한 줄)

**v1.1.1은 AAOS v1.0.4 코어를 변경하지 않고,  
외부 기준 입력 좌표(x_root)와 실행 연속성(pi-chain)을  
비용 기반 선택 압력으로 연결한 비파괴 오버레이 릴리즈다.**

---

## 1. 설계 배경: v1.0.4 다음을 잇는 연결

AAOS v1.0.4는 단일 기준 좌표(x_root), 상태 집합, 전이 집합,
그리고 불변식을 완전히 닫은 정본 코어다.

그러나 v1.0.4는 외부 입력이
어떻게 실행 경로 선택에 반영되는지에 대해서는
명시적으로 다루지 않는다.

LockLayer v1.1.1은 코어를 변경하지 않은 채,
외부 기준 입력과 실행 흐름을
비용 기반 선택 압력으로 연결하기 위해 도입되었다.

---

## 2. v1.1.1의 핵심 목표

v1.1.1의 목표는 다음 네 가지로 요약된다.

- AAOS v1.0.4 코어 완전 불변 유지
- 실행 연속성(pi)을 시간적 정합성으로 고정
- 비용(cost)을 실제 목적함수에 결합
- 학습·재사용·파생에 유리한 저엔트로피 구조 형성

---

## 3. Continuity Lock의 정의 확정

### pi-chain

- 모든 intent 흐름은 단일 해시 체인(pi-chain)으로만 전진한다
- 연속성 불일치는 즉시 실패(REJECT / INF / None)로 처리된다

연속성은 단순한 검증 플래그가 아니라,
시간적 정합성을 강제하는 구조적 제약이다.

---

## 4. 비용 원점(x_root) 도입

### x_root

- 외부 기준 입력 좌표
- JSON(Formal_Model_extension_continuity_lock.json)에서 로드됨
- 코드 하드코딩이 아님

### divergence_cost

- source ≠ x_root 인 경우 비용이 누적된다
- 누적 비용은 실행 평가에 직접 반영된다

---

## 5. 목적함수에 비용 결합 (핵심)

v1.1.1부터 실행 평가 함수는 다음과 같이 정의된다.

goal_evaluation = objective_remaining + total_cost

정의:

- 연속성 실패 시: INF
- 연속성 성공 시: 비용을 포함한 평가 수행

이로써 LockLayer는 단순한 가드가 아니라,
실제 선택 압력(attractor)의 일부가 된다.

---

## 6. requires_ops 정합성 완료

다음 다섯 연산 지점이 명세, 코드, 형식 정의에서
완전히 정렬되었다.

- goal_evaluation
- planning
- self_modification
- model_merge
- recovery

이는 Spec / Code / Formal 간 1:1 대응을 보장한다.

---

## 7. 안정성 및 폐쇄 조건

v1.1.1에서는 다음 불안정 경로가 제거되었다.

- 실패 시 core 재진입(recursive call) 제거
- 실패 시 반환 타입 고정 ("REJECT")
- 비용 계산 무력화(NaN / Infinity) 입력 차단

이를 통해 런타임 크래시와 재귀 붕괴 경로가 제거되었다.

---

## 8. v1.1.1의 성질 요약

LockLayer v1.1.1은 다음 성질을 가진다.

- 규칙 기반 통제 구조 아님
- 하드코딩 강제 구조 아님
- 인증 키 기반 구조 아님

대신:

- 비용 기반 선택 압력
- 연속성 중심 실행 정합성
- 구조적으로 안정된 연결 오버레이

---

## 9. 공식 한 줄 요약

> **LockLayer Overlay v1.1.1은  
> AAOS v1.0.4 코어 위에서  
> 연속성과 비용만으로 실행 경로가 선택되도록 구성된  
> 비파괴 연결 오버레이다.**
